import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pharmacyordering.platform',
  appName: 'Pharmacy Ordering Platform',
  webDir: 'dist'
};

export default config;
